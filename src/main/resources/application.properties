spring.application.name=zbhousereservas
server.port=8080
server.servlet.session.cookie.same-site=none

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:zbhouse_dev_db}
spring.datasource.username=${DATABASE_USER:felipe_dev}
spring.datasource.password=${DATABASE_PASSWORD:senha_teste}

spring.jpa.database=POSTGRESQL
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


#server.error.include-stacktrace=never
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

diaria.reserva.zbhouse=500.00
api.security.token.secret=${JWT_SECRET:1213456}
api.security.cookie.domain=${COOKIE_DOMAIN:localhost}

# CORS
api.security.cors.origins=http://localhost:5173,http://10.2.1.121:5173

